import { TicketsType } from '../../../Types/Types'

/* export const findAllCarriers = (tickets: TicketsType[]) => {
	const allDifferenceCarriers = new Set()
	return tickets.filter((ticket, index)) => indexOf() )
} */
